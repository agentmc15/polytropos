# Execution record

Planned orchestration: Astra. Planned and requested difficult implementation: Sol for central policy, execution, and pricing consumers. Routine documentation will use Terra. Runtime observations are recorded separately from those assignments; a requested spawn model alone is not proof of observed use.

Preflight: repository clean at a0cd570. Local Codex model/list confirms Astra, Sol, Terra, and Luna with their per-model effort support. No warm-pool management interface was found in the installed app-server schema; the CLI executor will use reserved recovery. No live model dispatch is used by tests.

T1 central routing and pricing implementation: worker reports 52 focused tests passed. T3 cost consumers: worker reports 179 tests passed across Codex/Copilot costing and journal consumers. Independent review remains pending.

Model evidence: the current orchestration rollout records Astra in turn_context. The three implementation rollouts created for this change record Sol in their new turn_context records after inherited history. These observed models are distinct from planned pins; assigned implementation roles are orchestrator records, not runtime-attested role metadata. Documentation dispatch requests Terra and awaits its own completion evidence.

Integration review found and corrected inclusive-input double charging, non-worker burn baselines, incomplete recovery identities, app-manifest transaction rollback, symlink traversal, stale acceptance evidence, and managed-role ownership interoperability. Sol workers independently cross-reviewed policy/application and execution changes. Terra completed documentation and the observed cache-token correction; Sol is handling the full-suite integration failures.

Application rollout: applied the managed configuration with backups outside the repository. Effective Codex app-server config/read reports the configured Astra orchestrator and Terra default worker. Both home and project verifier/phase-reviewer roles resolve to Sol with read-only sandboxing. App-policy status reports all ten destinations up to date. No live model task was invoked for this smoke check.

Validation limitation: the generic plugin-creator validator assumes a root skills/ directory and scans the independent Claude skill tree, so it does not support this monorepo's explicit codex/skills manifest layout. The twelve actual Codex skills individually pass the official quick validator; repository-specific plugin tests validate the configured path. Strict MkDocs build and generated Codex/doc drift checks passed before the final integration fixes; they will be repeated after those fixes.

Final orchestration acceptance: all five tasks are complete following independent cross-review and the full repository suite (3,411 tests, 171.194 seconds, OK with two conditional skips). Final migration wording passed 15 focused documentation checks. The final confirmation run passed the same 3,411 tests in 202.799 seconds, with two conditional skips: no excluded configured agent pin and no recorded frontier pin. Generated prompt/doc drift checks, all twelve native skill validations, and strict MkDocs build pass. The two skipped cases depend on Copilot preference states and do not skip Codex policy coverage.

Material limits: the runtime exposes no warm-pool management interface, so Astra is reserved. Polytropos centrally enforces its own dispatch; arbitrary host tools or explicitly chosen models outside that gate cannot be globally intercepted. Runtime model/role use remains unknown when independent telemetry is unavailable. Review reports retain a bounded excerpt with a digest, and review freshness requires a Git workspace. Screenshot rates are user-provided, not independently source-validated; cumulative usage cannot reconstruct per-request long-context charges. Claude pricing/configuration is unchanged. No live model dispatch was used in automated verification.

Publication and installed-plugin refresh follow this acceptance. The application configuration was already applied and verified against its backup: unrelated configuration remains equal and original guidance is retained verbatim.
