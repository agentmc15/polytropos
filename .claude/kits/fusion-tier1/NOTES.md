# NOTES — fusion-tier1

Cross-task learnings + the machine-readable outcome ledger this kit's scorecard consumes.

## Warm-cluster dogfood (Phase 1)
T1 -> T2 -> T3 (all editing skills/execute/SKILL.md, same sonnet pin) were served by ONE
continued implementer (warm sidekick) rather than three cold spawns — the exact pattern this
kit adds to /execute. Cache-warmth payoff was visible: T1 = 15 tool-uses / ~cold read of the
file; T2 = 5 tool-uses / 66s; T3 = 9 tool-uses / 70s. The file was read once and reused across
the cluster. The verifier is NEVER warmed — Phase-1 review uses fresh context.

## Outcome ledger
outcome: T1 model=sonnet attempts=1 result=pass review=clean
outcome: T2 model=sonnet attempts=1 result=pass review=clean
outcome: T3 model=sonnet attempts=1 result=pass review=clean
outcome: T4 model=opus attempts=1 result=pass review=clean
outcome: T5 model=opus attempts=1 result=pass review=clean
outcome: T6 model=sonnet attempts=1 result=pass review=clean
outcome: T8 model=haiku attempts=1 result=pass review=clean
outcome: T7 model=sonnet attempts=1 result=pass review=clean
